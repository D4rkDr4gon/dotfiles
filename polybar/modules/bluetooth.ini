[module/bluetooth]
type = custom/script
exec = /usr/bin/bash ~/.config/polybar/modules/bluetooth_status.sh
interval = 2

format = <label>
format-padding = 1

format-background = ${colors.chip-bluetooth}
format-foreground = ${colors.foreground}

click-left = blueman-manager &
